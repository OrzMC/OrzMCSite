---
title: "服务器地图大小限制"
summary: "运营一个PaperMC服务器，地图占用磁盘空间的大小，会随着玩家跑图越来越大。服务器需要定期备份地图，以防恶意玩家破坏服务器。不断增加的地图磁盘空间占用，对定期备份造成了比较大的影响。本文讨论一种可行的服务器地图备份时的磁盘占用优化方案。"
toc: true
weight: 31
tags: ["世界管理", "地图", "备份", "优化"]
categories: ["其他"]
date: 2025-08-02
---


---

## 地图文件夹的目录结构

### Vanilla 服务端的地图目录结构

一个典型的 Java 版世界存档文件夹内通常包含以下重要内容：
- `level.dat`: 该世界的全局核心信息，如世界名称、种子、游戏时间、天气周期等。
- `dimensions/minecraft/`: 维度容器，内部按 `{overworld,the_nether,the_end}/` 分别嵌套各维度数据（`region/` 区块数据 `.mca` 文件、`entities/`、`poi/`、`data/`）。
- `data/minecraft/`: 世界级数据（记分板、世界时钟、地图数据等）。
- `players/`: 玩家数据，内部 `players/{data,stats,advancements}/` 分别存储玩家 NBT、统计、进度。
- `generated/`: 游戏生成的结构数据。
- `datapacks/`: 数据包。

### PaperMC服务端的地图目录结构

Paper 26.1+ 的世界目录结构与 Vanilla 完全一致（单目录嵌套维度）：

```bash
world/
├── level.dat                        # 世界核心数据
├── data/                            # 世界级数据（data/minecraft/ 命名空间）
├── players/                         # 玩家数据（data/ stats/ advancements/ 合并于此）
├── generated/
├── datapacks/
└── dimensions/minecraft/
    ├── overworld/                   # 主世界：region/ entities/ poi/ data/
    ├── the_nether/                  # 下界
    └── the_end/                     # 末地
```

---

## 如何计算服务器地图占用磁盘的大小

默认情况下，Minecraft 服务器配置文件 `server.properties` 中指定：

```bash
max-world-size=29999984 # 单位为方块，表示地图的最大半径
max-build-height=256    # 单位为方块，表示地图的最大高度
```

Minecraft 地图是正方形的，按 **区块 (chunk)** 为单位存储。一个区块的默认尺寸为 16x16x256（长 x 宽 x 高），也就是一个区块存放 **65536** 个方块。理论上地图总共会有：

((2 * max-world-size) ^ 2) * max-build-height = 921599016960262144 

个方块，也就是 14062485000004 个区块。

按照一个高度为 256 的区块在磁盘中的存储大小约为 4MB 来计算，共需要磁盘空间约为 53,644,124 GB。


所以，要开一个可维护的 Minecraft 服务器，需要控制好磁盘使用，并设置磁盘容量上限。按目前云服务器一般配备 50GB 磁盘空间来算，除去 10GB 系统占用，可供开服存储地图的磁盘空间仅有 40GB。按地图默认高度 `max-build-height=256` 计算，地图最大半径值为 `max-world-size=810`（假设玩家会经过所有方块的理想情况）。显然这种计算不符合实际情况。假设玩家只会经过 10% 的区块，那么 `max-world-size=2561`。这同样无法反映实际情况，因此需要统计区块使用率，用来指导设置世界大小。


---

## 地图备份文件占用磁盘太大解决方案

目前服务器托管平台，提供自动优化地图大小的能力。地图中玩家没有呆过的区块会被不定时的清理掉，保证地图文件的大小可控。
如果你的地图文件太大，可以使用工具 [mcaselector](https://github.com/Querz/mcaselector) 进行区域按条件筛选后，
进行删除，可以显著减小地图文件占用空间。

[mcaselector 下载地址](https://github.com/Querz/mcaselector/releases)

可以使用CLI命令行模式进行操作，参考文档：[mcaselector CLI 模式](https://github.com/Querz/mcaselector/wiki/CLI-Mode)

备份地图的命令：

```bash
# 仅导出玩家待了5分钟以上的方块
java -jar                           \
    mcaselector-2.5.3.jar           \
    --mode export                   \
    --query "InhabitedTime > 5min"  \
    --world <directory_of_world>    \
    --output-world <directory_for_backup>
```

```bash
# 在原始地图上直接操作，删除所有玩家没待满5分钟以上的方块
java -jar                           \
    mcaselector-2.5.3.jar           \
    --mode delete                   \
    --query "InhabitedTime <= 5min" \
    --world <directory_of_world>
```

CLI命令使用参数速查图：

![mcaselector cheatsheet](https://gist.githubusercontent.com/Querz/5e08c4ab863c2ad8b5da146dc4188ecb/raw/efc4b6990bce79ed4512e9a0a363c64afd7b7444/commands-diagram.png)
